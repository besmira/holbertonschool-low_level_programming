#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @j: the number checked
 * Return: j if number is positive, -j is number is negative
 */
int _abs(int j)
{
	if (j > 0)
		return (j);
	else
		return (-j);
}
