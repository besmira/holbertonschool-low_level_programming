#include "main.h"
/**
 * add - add two integers
 * @x: first number
 * @y: second number
 * Return: result returns the result of adding two integers
 */
int add(int x, int y)
{
	int result;

	result = x + y;
	return (result);
}
