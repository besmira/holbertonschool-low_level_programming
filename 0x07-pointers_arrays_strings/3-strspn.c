#include "main.h"
/**
 * _strspn - gets the length of a prefix substring
 * @s: string
 * @accept: bytes
 * Returns the number of bytes in the initial segment of s 
 * whith only of bytes from accept
 */
 
