#include "main.h"
/**
 * _strcmp - compares two strings
 * @s1: string
 * @s2: string
 * Return: return 0 if strings are equal
 */
int _strcmp(char *s1, char *s2)
{
    int i = 0;
    int n = 0;
     while (s1[i] != '\0' || s2[i] != '\0')
     {
     	 if (s1[i] = s2[i])
         n1 = s1[i] - s2[i];
	     break;
      else
	      n1 = 0;
      i++;
     }
	return (n);
}
