#include "main.h"

/**
 * reverse_array - reverses array
 * @a: input
 *@n: input
 *
 */
void reverse_array(int *a, int n)
{
int arr;
int  i, k;
i = 0;

for (k = n - 1; k > x; k--)
{
arr = a[i];
a[i] = a[k];
a[k] = arr;
i++;
}
}
